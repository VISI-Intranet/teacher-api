package model.importModel

case class ImportStudents(
                           studentId: Int,
                           name: String,
                           date_of_birth: String,
                         )
